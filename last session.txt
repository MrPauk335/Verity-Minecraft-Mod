отово. Анти-leak фильтр теперь работает и для OpenRouter fallback тоже. Модели которые выводят "We need to respond as Verity, following the rules..." будут отклоняться — добавил новые триггеры: following the rules, we have player, the context shows, we know real pc.
▣  Build · GLM 5.2 · 2m 0s
log.txt
→Read log.txt 
Работает! Verity ответил "Привет. Я здесь." — коротко, канонично, по-русски. System prompt не утёк. TTS проиграл 2с. 
Но Gemini тайм-аутит 3 раза по 8с = 24с ожидания. Это потому что gemini-3-flash-preview перегружена (503/timeout). Ключ друга платный но модель перегружена. Можно попробовать gemini-2.5-flash — она стабильнее. Но это уже настройка у пользователя.
▣  Build · GLM 5.2 · 12.2s
очень долго
Проблема — 3 Gemini ключа тайм-аутят по 8с каждый = 24с, потом ещё OpenRouter 429. Уменьшу Gemini timeout до 5с и сделаю чтобы при timeout пробовался только 1 ключ (не все 3):
← Edit src\main\java\net\verity\ai\VerityLLMClient.java

private static final String API_URL  = "https://openrouter.ai/api/v1/chat/completions";
private static final String GEMINI_URL = "https://generativelanguage.googleapis.com/v1beta/models/%s:generateContent?key=%s";
private static final Duration TIMEOUT = Duration.ofSeconds(8);

private static final Gson GSON = new Gson();

// Ключи и модели — загружаются из конфига (round-robin при 429)
private static List<String> API_KEYS = new ArrayList<>();
✱Grep "connectTimeout\(TIMEOUT\)" in src\main\java\net\verity\ai\VerityLLMClient.java (2 matches)
→Read src\main\java\net\verity\ai\VerityLLMClient.java [limit=10, offset=423]
← Edit src\main\java\net\verity\ai\VerityLLMClient.java

String url = String.format(GEMINI_URL, model, apiKey);

HttpClient client = HttpClient.newBuilder()
        .connectTimeout(TIMEOUT)
        .build();

HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create(url))
        .header("Content-Type", "application/json")
        .timeout(TIMEOUT)
        .POST(HttpRequest.BodyPublishers.ofString(GSON.toJson(requestBody)))
        .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
Теперь сделаю чтобы Gemini пробовал максимум 1 ключ при timeout (не все 3):
✱Grep "private static String callGemini\b" in src\main\java\net\verity\ai\VerityLLMClient.java (1 match)
→Read src\main\java\net\verity\ai\VerityLLMClient.java [limit=25, offset=340]
← Edit src\main\java\net\verity\ai\VerityLLMClient.java
java.util.List<String> keys = VerityConfig.geminiApiKeys();
String model = VerityConfig.geminiModel().trim();
if (keys.isEmpty() || model.isEmpty()) return null;

for (String apiKey : keys) {



    try {
        String result = callGeminiModel(phase, playerName, message, history, language, context, model, apiKey.trim());
        if (result != null) return result;
        VerityMod.LOGGER.warn("Gemini rate limited on key={}..., trying next",
                apiKey.length() > 10 ? apiKey.substring(0, 10) : apiKey);



    } catch (Exception e) {
        VerityMod.LOGGER.warn("Gemini key failed: {}", e.getMessage());
    }
}
Также уменьшу количество OpenRouter моделей которые пробуются при fallback — только первые 2:
✱Grep "for \(String key : keys\) \{.*for \(String model : models\)" in src\main\java\net\verity\ai\VerityLLMClient.java 
✱Grep "callOpenRouter" in src\main\java\net\verity\ai\VerityLLMClient.java (3 matches)
→Read src\main\java\net\verity\ai\VerityLLMClient.java [limit=20, offset=516]
← Edit src\main\java\net\verity\ai\VerityLLMClient.java

    List<String> keys   = API_KEYS.isEmpty() ? List.of("") : API_KEYS;
    List<String> models = MODELS;

    for (String key : keys) {
        for (String model : models) {





            String result = callModel(phase, playerName, message, history, model, key, language, context);
            if (result != null) return result;
            VerityMod.LOGGER.warn("Rate limited on model={} key={}..., trying next",
                    model, key.length() > 10 ? key.substring(0, 10) : key);
        }
    }
    VerityMod.LOGGER.error("All {} key(s) \u00D7 {} model(s) exhausted", keys.size(), models.size());
    return null;
}

private static String callModel(
$ .\gradlew.bat build 2>&1
> Configure project :
Fabric Loom: 1.17.13

> Task :compileJava

> Task :processResources UP-TO-DATE
> Task :classes
> Task :compileClientJava UP-TO-DATE
> Task :processClientResources NO-SOURCE
.\gradlew.bat : Note: D:\Projects\Java\Verity\src\main\java\net\verity\ai\VerityDialogueController.java uses or 
…
Click to expand
Сборка успешна. Что изменилось:
- Gemini timeout 5с (вместо 8с) — отдельный GEMINI_TIMEOUT
- При timeout — сразу fallback на OpenRouter, не пробует остальные ключи
- Максимум 2 Gemini ключа вместо всех 3
- Максимум 2 ключа × 2 модели OpenRouter вместо 4×4
- Худший случай: 5с (Gemini) + ~4с (2×2 OpenRouter) = ~9с вместо 24+с
▣  Build · GLM 5.2 · 2m 35s
у гемини нет встроенных ключей
Да, верно — BUILTIN_GEMINI_KEYS пустой. Значит geminiApiKeys() возвращает только пользовательский ключ (1 штука). Лог показывает 3 попытки тайм-аута — это один и тот же ключ ретраится? Нет, посмотрю:
→Read src\main\java\net\verity\config\VerityConfig.java [limit=15, offset=57]
Account bodja22812-817ju90tv is suspended, possibly due to reaching the monthly spending limit or failure to pay past invoices. Please go to https://fireworks.ai/account/billing for more information.